/*
function calculateTax(amount, tax = 15) {
    let total = amount + (amount / 100) * tax;

    return total;
}

calculateTax(1000, 20);

function getFullName(firstName, lastName, middleName = "") {
    return firstName + " " + lastName + " " + middleName;
}*/
